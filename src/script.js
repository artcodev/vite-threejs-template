import ThreeScene from './classes/ThreeScene';

// Make sure the DOM is loaded before creating the scene
document.addEventListener('DOMContentLoaded', () => {
    const threeScene = new ThreeScene();
});
